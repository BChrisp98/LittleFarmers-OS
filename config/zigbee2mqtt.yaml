version: 5

mqtt:
  base_topic: zigbee2mqtt
  # FIXED (2026-08-02): this is now an Elastic IP, permanently attached to
  # the EC2 instance. It replaces two prior dynamic IPs that both broke on
  # instance restart (63.178.68.2, then 18.193.124.166) - this one will not
  # change again. See Vendor-Liste.md for the history.
  server: mqtt://63.184.28.93:1883

frontend:
  enabled: true
  port: 8080
  host: 0.0.0.0

advanced:
  log_level: info
  network_key: GENERATE
  pan_id: GENERATE
  ext_pan_id: GENERATE

homeassistant:
  enabled: false